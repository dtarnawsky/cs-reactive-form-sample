import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ticket-40887',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
